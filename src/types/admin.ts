export type User = {
  _id: string;
  name: string;
  email: string;
  business: string;
  status: string;
  plan: string;
  createdAt: string;
  updatedAt: string;
  profilePicUrl: string;
  expiresAt: string;
};
